import styled from "styled-components"

export const StyledPhotoComments = styled.div`
  .comments {
    overflow-y: auto;
    word-break: break-word;
    padding: 0.5rem;
  }

  .comments li {
    margin-bottom: 0.5rem;
    line-height: 1.2;
  }
`